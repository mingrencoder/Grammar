#ifndef MATCH_H
#define MATCH_H

// #include "Time.h"
//先声明接下来要用到的Time类
class Time;

class Match
{
public:
	// Match();
	// Match(int hour, int min, int sec);
	void printMatchTime(Time &t);
	// void testTime();
private:
	// Time m_tTimer;
};

#endif